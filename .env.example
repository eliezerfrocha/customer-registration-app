# Usado pelo docker-compose para provisionar o Postgres e compor a DATABASE_URL da API.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=customer_registration

# URL pública da API, usada para o build do frontend (fica embutida no bundle estático).
VITE_API_URL=http://localhost:3333
